<Window x:Class="EventBookingSystem.Desktop.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Panel" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="WelcomeText" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Create Event" Padding="10,5" Margin="0,0,10,0" Click="CreateEventButton_Click"/>
            <Button Content="Delete Event" Padding="10,5" Margin="0,0,10,0" Click="DeleteEventButton_Click"/>
            <Button Content="Refresh List" Padding="10,5" Margin="0,0,10,0" Click="RefreshButton_Click"/>
            <Button Content="Pending Bookings" Padding="10,5" Margin="0,0,10,0" Click="PendingBookingsButton_Click"/>
            <Button Content="Reports" Padding="10,5" Margin="0,0,10,0" Click="ReportsButton_Click"/>
            <Button Content="Logout" Padding="10,5" Click="LogoutButton_Click"/>
        </StackPanel>

        <ListBox x:Name="EventsListBox" Grid.Row="2"/>
    </Grid>
</Window>